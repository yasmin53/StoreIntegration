spring:
  application :
    name: store-service
    
  datasource:
    url: jdbc:mysql://localhost:3306/usecase
    
    username: root
    
    password: root
    
  jpa:
    show-sql: true
    
    hibernate:
      ddl-auto: update
server:
  port: 8001
  
management:  endpoints:    web:      exposure:        include: "*"       
   